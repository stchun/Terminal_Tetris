# Terminal Tetris — 개선 로드맵 (Roadmap)

## 📌 우선순위별 정리

---

### ✅ P0 — 안정성 강화 (완료)

#### 1. 터미널 크기 부족 경고 메시지 ✅
- **변경**: `draw()` 함수에서 모든 `try/except curses.error` → `_draw_safe()` 헬퍼로 대체, 에러 발생 시 경고 오버레이 표시 ("터미널 크기가 충분하지 않습니다")
- **동작**: Q로 종료 또는 R으로 재시작 가능

#### 2. 게임 루프 안정화 ✅
- **변경**: Hold 시 유효치 않은 경우, 좌우 오프셋(-1/+1)을 시도 → 모두 실패 시 게임 오버 처리 (Q/R/L 가능)

---

### 🟡 P1 — 구조적 개선 (코드 가독성/유지보수)

#### 3. `main()` 함수 분리
- **현재**: 모든 로직이 `main(stdscr)` 한 함수에 600줄
- **개선**: 입력 처리 / 물리 엔진 / 렌더링을 별도 함수로 분리
- **예상 구조**:
  ```python
  def input_handler(stdscr, state): ...   # 키 입력 처리
  def gravity_tick(board, shape, px, py, level): ...  # 중력 & 고정
  def renderer(stdscr, board, piece, ...): ...  # 화면 렌더링
  ```

#### 4. 게임 상태 Dataclass 도입
- **현재**: 모든 상태가 `draw()` 인자로 전달 (10개 파라미터!)
- **개선**: `@dataclass`로 묶어 타입 안정성 향상
- **예상 구조**:
  ```python
  @dataclass
  class GameState:
      board, piece, next_piece, hold_piece, ...
  ```

#### 5. 문서 vs 코드 일치성 수정
- **현재**: `analysis.md`에서 "왼쪽 패널: Hold / 오른쪽 패널: Stats + Next"로 기술됨
- **실제**: 왼쪽에 Hold, 오른쪽에 **Stats + Next** (Next가 왼쪽이 아닌 오른쪽)
- **개선**: 
  - 문서 (`analysis.md`) 수정 → Next 패널 위치 정확히 명시
  - 또는 코드 상수명 변경 (`LEFT_W` → `HOLD_PANEL_WIDTH`, `RIGHT_PANEL_WIDTH` 등)

---

### 🟢 P2 — 기능 추가 (향후 구현)

#### 6. SRS (Super Rotation System) 정식 월 킥 테이블 적용
- **현재**: `{0, -1, +1, -2, +2}` 순차 오프셋만 시도
- **개선**: SRS 표준 월 킥 테이블 적용 (Tetris World Championship 기준)
- **참고**: https://www.tetris.com/rules

#### 7. 하드 드롭 점수 최적화
- **현재**: `hard_drop_cells * 2`점 / 셀
- **참고**: 공식 Tetris 규칙은 하드 드롭 = 0점 (일부 모드 제외)
- **개선 선택**: 현재 시스템 유지 or 공식 규칙에 맞춤

#### 8. 게임 재시작 로직 정리
- **현재**: `R` 키로 outer while 루프 반환 후 `_bag = []` 초기화
- **개선**: 명시적 함수(`restart_game()`)으로 분리하여 가독성 향상

---

### 🟣 P3 — 테스트 및 품질

#### 9. 단위 테스트 추가
- `is_valid()` — 유효하지 않은 위치, 경계 초과, 겹침 등 케이스
- `clear_lines()` — 줄 제거 시나리오 (0줄, 1줄, 테트리스)
- `rotate_cw()` — 회전 각도 정확성 검증
- `random_piece()` — 7-bag 순서 보장 테스트

#### 10. 코드 리뷰 체크리스트
- [ ] 모든 함수의 독백 문서화 (`docstring`) 확인
- [ ] 무한 루프 가능성 검토 (Hold → 게임 오버 처리 경로)
- [ ] 터미널 크기 변화에 따른 레이아웃 리스폰스성 검증

---

### 📊 우선순위 매트릭스

| # | 항목 | 분류 | 노력도 | 영향력 | 우선순위 |
|---|------|------|--------|--------|---------|
| 1 | 터미널 크기 경고 | 안정성 | 낮음 | 높음 | 🔴 P0 |
| 2 | Hold 스폰 실패 처리 | 안정성 | 낮음 | 중간 | 🔴 P0 |
| 3 | main() 분리 | 구조 | 중등도 | 높음 | 🟡 P1 |
| 4 | GameState dataclass | 구조 | 중등도 | 높음 | 🟡 P1 |
| 5 | 문서/코드 일치 수정 | 구조 | 낮음 | 중간 | 🟡 P1 |
| 6 | SRS 월 킥 테이블 | 기능 | 높음 | 높음 | 🟢 P2 |
| 7 | 하드 드롭 점수 규칙 | 기능 | 낮음 | 낮음 | 🟢 P2 |
| 8 | restart_game() 분리 | 구조 | 낮음 | 중간 | 🟡 P1 |
| 9 | 단위 테스트 추가 | 품질 | 중등도 | 높음 | 🟣 P3 |
| 10 | 코드 리뷰 체크리스트 | 품질 | 낮음 | 중간 | 🟣 P3 |

---

### 🗓️ 예상 타임라인 (개인 개발 기준)

| 단계 | 항목 | 예상 소요 |
|------|------|----------|
| Phase 1 (안정화) | #1, #2, #5 | ~0.5시간 |
| Phase 2 (구조 개선) | #3, #4, #8 | ~1~2시간 |
| Phase 3 (기능 확장) | #6 | ~3~4시간 |
| Phase 4 (품질) | #9, #10 | ~1시간 |

> **현재 프로젝트 상태**: 기능 완성도는 매우 높음. P1/P2 항목은 코드 품질과 사용자 경험 향상을 위한 것이며, 현재 플레이에 지장이 없는 수준의 개선입니다.
